/* This is the default config, should work fine
 * for all the users, but in case it doesnt for
 * you just change the values below.
 * Uid can be obtained by
 *      id -u <your user name>
 * Gid can be obtained by
 *      id -g <your user name>
 */

#define CONFIG_USER_UID  1000
#define CONFIG_USER_GID  100
